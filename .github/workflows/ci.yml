name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  actions: read
  security-events: write

# Validates the static-analysis and security workflows against examples/hello/.
#
# tf-sonar requires SONAR_TOKEN to be configured in the repository. The job
# is skipped on fork pull requests (secrets are not available to forks), but
# runs on all other events and fails if the token is absent.
#
# The AWS-backed workflows (tf-plan/apply/destroy/drift/cost) are exercised in
# `dry-run: true` mode so the workflow wiring is continuously validated without
# requiring live infrastructure or secrets.

jobs:
  workflows-policy:
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-workflows-policy.yml@main

  codeql-actions:
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-codeql.yml@main
    with:
      languages: actions
      source-root: .
      upload-sarif: false

  fmt:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-fmt.yml@main
    with:
      working-directory: examples/hello

  validate:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-validate.yml@main
    with:
      chdir: examples/hello/stack

  lint:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-lint.yml@main
    with:
      lint-path: examples/hello
      tflint-config: examples/hello/.tflint.hcl

  docs:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-docs.yml@main
    with:
      working-directory: examples/hello
      output-file: README.md

  security:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-security.yml@main
    with:
      scan-ref: examples/hello
      upload-sarif: false

  tfsec:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-tfsec.yml@main
    with:
      scan-path: examples/hello

  checkov:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-checkov.yml@main
    with:
      scan-directory: examples/hello

  test-tidy:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-test-tidy.yml@main
    with:
      test-dir: examples/hello/test

  semgrep:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-semgrep.yml@main
    with:
      working-directory: examples/hello

  sonar:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-sonar.yml@main
    with:
      working-directory: examples/hello
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  plan-dry-run:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-plan.yml@main
    with:
      environment: dev
      chdir: examples/hello/stack
      dry-run: true

  drift-dry-run:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-drift.yml@main
    with:
      environment: dev
      chdir: examples/hello/stack
      dry-run: true

  apply-dry-run:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-apply.yml@main
    with:
      environment: dev
      chdir: examples/hello/stack
      dry-run: true

  destroy-dry-run:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-destroy.yml@main
    with:
      environment: dev
      chdir: examples/hello/stack
      dry-run: true

  cost-dry-run:
    uses: FelipeFuhr/ffreis-workflows-terraform/.github/workflows/tf-cost.yml@main
    with:
      path: examples/hello/stack
      dry-run: true
