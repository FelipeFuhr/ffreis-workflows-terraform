package test

import (
	"testing"
)

// Placeholder for Terratest integration tests.
// These require a Terraform backend and provider credentials.
// Run with: go test -v -timeout 30m ./...
func TestHelloStack(t *testing.T) {
	t.Skip("integration test: requires terraform init and provider access")
}
